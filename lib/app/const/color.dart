import 'package:flutter/material.dart';

class ConstColors {
  static const primaryButton = Colors.redAccent;
  static const primaryText = Colors.white;
  static const secondaryText = Colors.grey;
  static const clickedPrimaryButton = Colors.red;
  static const resultArgument = Colors.greenAccent;
  static const mainBackground = Color.fromARGB(255, 64, 55, 55);
  static const containerBackground = Color.fromARGB(255, 101, 87, 87);
}
